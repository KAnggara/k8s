apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coba.fullname" . }}-test-connection"
  labels:
    {{- include "coba.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coba.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
