apiVersion: v1
kind: Service
metadata:
  name: {{ include "coba.fullname" . }}
  labels:
    {{- include "coba.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coba.selectorLabels" . | nindent 4 }}
